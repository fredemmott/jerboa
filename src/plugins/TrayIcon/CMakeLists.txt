SET(
    SOURCES
    TrayIcon.cpp
)
SET(
    HEADERS
    TrayIcon.h
)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})
QT4_ADD_RESOURCES(RESOURCE_SOURCES TrayIcon.qrc)
QT4_WRAP_CPP(MOC_SOURCES ${HEADERS})
ADD_LIBRARY(
    TrayIcon
    ${SOURCES}
	${RESOURCE_SOURCES}
    ${MOC_SOURCES}
)

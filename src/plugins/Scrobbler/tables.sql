CREATE TABLE `LastFMCache` (
	`TimeStamp`INT NOT NULL,
	`Length` INT NOT NULL,
	`Artist` TEXT NOT NULL,
	`Album` TEXT NOT NULL,
	`Name` TEXT NOT NULL,
	`TrackNumber` INT NOT NULL,
	`MBID` TEXT NOT NULL
);
